{
  "name": "link-previewer",
  "version": "1.1.1",
  "description": "Preview links in a popup without leaving your current page",
  "private": true,
  "scripts": {
    "build": "cd build && ./build.sh",
    "clean": "cd build && ./build.sh clean"
  }
}
